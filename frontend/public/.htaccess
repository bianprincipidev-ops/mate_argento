<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !%2dF
  RewriteCond %{REQUEST_FILENAME} !%2dd
  RewriteRule . /index.html [L]
</IfModule>